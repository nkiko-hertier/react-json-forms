// Main component
export { FormRenderer } from "./renderer/FormRenderer";

// Schema types (VERY important for DX)
export type {
  FormSchema,
  FormSection,
  FormField,
  VisibilityRule,
  ChoiceOption
} from "./schema/types";
